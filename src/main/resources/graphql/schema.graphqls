type Article {
    id: ID
    title: String
    text: String
    thumbnail: String
    comments: [Comment]
}

type Comment {
    text: String
}

type Query {
    getArticles: [Article]
    getArticle(id: ID): Article
    getArticleComments(id: ID): [Comment]
}